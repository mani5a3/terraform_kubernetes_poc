---
apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  labels:
    app: myapp
spec:
  selector:
    app: myapp
  type: NodePort 
  ports:
    - targetPort: 80 # container
      port: 80  # service 
      nodePort: 30008 